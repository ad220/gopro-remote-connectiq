import Toybox.Lang;
import Toybox.WatchUi;
import Toybox.System;
import Toybox.Test;


(:test)
module TogglablesDelegateTest {

    (:test)
    function testInit(logger as Test.Logger) as Boolean {
        // check if available settings have been retrieved
        
        return true;
    }
    
    (:test)
    function testBrowsing(logger as Test.Logger) as Boolean {
        // test browsing the view with up/down keys, onSelectable events
        return true;
    }
    
    (:test)
    function testOnFlicker(logger as Test.Logger) as Boolean {

        return true;
    }

    
    (:test)
    function testOnLed(logger as Test.Logger) as Boolean {

        return true;
    }

    
    (:test)
    function testOnStabilize(logger as Test.Logger) as Boolean {

        return true;
    }

    
    (:test)
    function testOnGps(logger as Test.Logger) as Boolean {

        return true;
    }

    
    (:test)
    function testOnPower(logger as Test.Logger) as Boolean {

        return true;
    }
}